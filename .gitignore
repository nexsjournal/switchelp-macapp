node_modules/
dist/
target/
.DS_Store
.env
.env.*
!.env.example
coverage/
test-results/
playwright-report/
.local/

# 本机代理工具的工作目录：计划草稿、技能定义等，都是本机工作记录，不进仓库。
# 其中值得版本化的两样已经搬出来了：审计脚本在 `scripts/in-page-audit.js`，
# 审计方法与已知基线在 `docs/development/04-design-conformance.md`。
.zcode/
# 同上的工具配置（排除清单本身），也是本机的东西。
.zcodeignore

# 第三方产品的界面参考截图：用户提供的本地调研素材，不随仓库再分发。
# 若要随仓库分发，请先确认这些截图的使用授权，然后删除下面这一行。
referimg/
*.log
dist-release/
src-tauri/binaries/
